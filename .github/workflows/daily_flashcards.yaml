name: Daily Flashcard Generator

on:
  schedule:
    - cron: '0 16 * * *'  # 00:00 HKT (UTC+8)
  workflow_dispatch:
  repository_dispatch:
    types: [advance_chapter, flashcard_grade, anki_review, pdf_completed]
  push:                   # <-- ADD THIS: Triggers an auto-build anytime you push changes
    branches:
      - main
    paths:
      - 'config.json'
      - 'generate_feeds.py'
      - 'anki_scheduler.py'
      - 'reviewer.html'
      - 'reviewer_app.js'
      - 'reviewer_state.js'
      - '*.csv'

concurrency:
  group: flashcard-sync
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Dependencies
        run: |
          pip install feedgen
      
      - name: Run Feed Engine
        run: |
          python generate_feeds.py
      
      - name: Commit & Push Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add *.xml history.json config.json cards/
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update daily feeds and histories" && git push)
